spring:
  application:
    name: cloud-leaf-sms
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.56.10:80
      config:
        file-extension: yml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
#        shared-configs: